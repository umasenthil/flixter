<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
	<h1 class="text-center"><%= @course.title %> </h1>
	<br />
	  <% if current_user && current_user == @course.user %>
      <br />
      <br />
      <%= link_to 'Student page', course_path(@course), :class => 'btn btn-danger btn-large' %>
    <% end %>
</div>
